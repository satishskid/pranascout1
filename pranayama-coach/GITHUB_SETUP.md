# 🚀 Git Setup and GitHub Deployment Guide\n\n## Step 1: Initialize Git Repository\n\n```bash\n# Initialize git repository (if not already done)\ncd /path/to/pranayama-coach\ngit init\n\n# Add all files\ngit add .\n\n# Create initial commit\ngit commit -m \"Initial Pranayama Coach application with Netlify deployment\"\n```\n\n## Step 2: Connect to GitHub Repository\n\n```bash\n# Add your GitHub repository as remote\ngit remote add origin https://github.com/satishskid/pranascout.git\n\n# Push to GitHub\ngit branch -M main\ngit push -u origin main\n```\n\n## Step 3: Set Up Netlify Auto-Deploy\n\n### Option A: GitHub Integration (Recommended)\n\n1. **Go to Netlify Dashboard**\n   - Visit [netlify.com](https://netlify.com)\n   - Click \"New site from Git\"\n\n2. **Connect GitHub**\n   - Choose \"GitHub\" as your Git provider\n   - Authorize Netlify to access your repositories\n   - Select `satishskid/pranascout` repository\n\n3. **Configure Build Settings**\n   ```\n   Base directory: web-dashboard\n   Build command: npm run build\n   Publish directory: web-dashboard/build\n   ```\n\n4. **Set Environment Variables**\n   In Netlify dashboard → Site settings → Environment variables:\n   ```\n   REACT_APP_API_URL=/api\n   REACT_APP_ENVIRONMENT=production\n   REACT_APP_APP_NAME=Pranayama Coach Admin\n   REACT_APP_VERSION=1.0.0\n   CI=false\n   ```\n\n5. **Deploy**\n   - Click \"Deploy site\"\n   - Netlify will automatically build and deploy\n\n### Option B: Manual Upload (Quick Fix)\n\n1. **Build Locally**\n   ```bash\n   cd web-dashboard\n   npm install\n   CI=false npm run build\n   ```\n\n2. **Upload to Netlify**\n   - Go to Netlify dashboard\n   - Drag and drop the `build` folder\n\n## Step 4: Update Backend URL (Important!)\n\nUpdate the backend URL in your configuration files:\n\n### In `web-dashboard/netlify.toml` (line 23):\n```toml\nto = \"https://your-actual-backend-url.up.railway.app/api/:splat\"\n```\n\n### In `web-dashboard/public/_redirects` (line 2):\n```\n/api/*  https://your-actual-backend-url.up.railway.app/api/:splat  200\n```\n\n## Step 5: Test Your Deployment\n\n### URLs to Test:\n- `https://your-site.netlify.app/` → Should show login page\n- `https://your-site.netlify.app/dashboard` → Should redirect to login\n- `https://your-site.netlify.app/users` → Should redirect to login\n\n### Check for Issues:\n1. Open browser developer tools (F12)\n2. Check Console tab for JavaScript errors\n3. Check Network tab for failed API calls\n4. Test authentication flow\n\n## Step 6: Set Up Continuous Deployment\n\nOnce connected to GitHub, every push to `main` branch will:\n1. Automatically trigger a new build\n2. Deploy to your Netlify site\n3. Update your live application\n\n### Deployment Workflow:\n```bash\n# Make changes to your code\n# ...\n\n# Commit and push\ngit add .\ngit commit -m \"Your changes description\"\ngit push origin main\n\n# Netlify will automatically deploy!\n```\n\n## 🔧 Troubleshooting Common Issues\n\n### Build Fails on Netlify\n\n**Problem:** Build fails with module errors\n**Solution:**\n1. Add `CI=false` to environment variables\n2. Check Node.js version is 18+\n3. Clear cache and retry deploy\n\n### 404 Errors on Direct URLs\n\n**Problem:** Routes like `/dashboard` show 404\n**Solution:**\n1. Check `_redirects` file is in build folder\n2. Verify netlify.toml configuration\n3. Test SPA redirect rules\n\n### API Calls Fail\n\n**Problem:** Login/data loading fails\n**Solution:**\n1. Update backend URL in netlify.toml\n2. Check CORS settings on backend\n3. Verify environment variables\n\n### Deployment Takes Too Long\n\n**Problem:** Build times out\n**Solution:**\n1. Clear Netlify build cache\n2. Check for large dependencies\n3. Optimize build process\n\n## 📋 Pre-Deployment Checklist\n\n- [ ] Code committed to GitHub\n- [ ] Repository connected to Netlify\n- [ ] Build settings configured correctly\n- [ ] Environment variables set\n- [ ] Backend URL updated in config files\n- [ ] Local build test successful\n- [ ] _redirects file in place\n- [ ] HTTPS enabled on Netlify\n\n## 🎯 Advanced Configuration\n\n### Custom Domain Setup\n1. Buy a domain (e.g., pranayamacoach.com)\n2. Add custom domain in Netlify settings\n3. Configure DNS records\n4. Enable HTTPS (automatic)\n\n### Branch Previews\n- Create feature branches for testing\n- Netlify automatically creates preview URLs\n- Test changes before merging to main\n\n### Form Handling (Future)\n- Netlify can handle contact forms\n- Add `netlify` attribute to forms\n- Configure form notifications\n\n---\n\n## 🚀 Quick Commands Summary\n\n```bash\n# Initial setup\ngit init\ngit add .\ngit commit -m \"Initial commit\"\ngit remote add origin https://github.com/satishskid/pranascout.git\ngit push -u origin main\n\n# Fix and redeploy\ncd web-dashboard\nrm -rf build node_modules\nnpm install\nCI=false npm run build\n\n# Test locally\nnpx serve -s build\n```\n\nYour Pranayama Coach dashboard will be live and automatically deployed! 🎉