# 🚨 Netlify 404 Error - Quick Fix Guide\n\n## Problem\nYou're seeing a \"Page not found\" error on Netlify because the single-page application (SPA) routing isn't configured correctly.\n\n## ⚡ Quick Fix Solutions\n\n### Option 1: Use the Fix Script (Recommended)\n\n```bash\n# Run the automated fix script\n./scripts/fix-deploy.sh\n```\n\nThis script will:\n- Clean and rebuild your dashboard\n- Check for common issues\n- Prepare files for deployment\n- Give you deployment options\n\n### Option 2: Manual Fix\n\n1. **Check your Netlify build settings:**\n   - Base directory: `web-dashboard`\n   - Build command: `npm run build`\n   - Publish directory: `web-dashboard/build`\n\n2. **Rebuild and redeploy:**\n   ```bash\n   cd web-dashboard\n   rm -rf build node_modules\n   npm install\n   CI=false npm run build\n   ```\n\n3. **Deploy using one of these methods:**\n\n   **Method A: Netlify CLI**\n   ```bash\n   npm install -g netlify-cli\n   netlify login\n   netlify deploy --prod --dir=build\n   ```\n\n   **Method B: Drag & Drop**\n   - Go to your Netlify dashboard\n   - Drag the `web-dashboard/build` folder to the deploy area\n\n   **Method C: GitHub Auto-Deploy**\n   - Push your code to GitHub\n   - Netlify will automatically rebuild\n\n## 🔧 Configuration Files Fixed\n\nI've updated these files to fix the routing:\n\n### `web-dashboard/netlify.toml`\n- ✅ Proper SPA redirect configuration\n- ✅ API proxy setup\n- ✅ Security headers\n- ✅ Caching rules\n\n### `web-dashboard/public/_redirects`\n- ✅ Backup redirect rules\n- ✅ SPA fallback to index.html\n\n## 🐛 Common Issues & Solutions\n\n### Issue 1: Build Failures\n**Symptoms:** Build fails with errors\n**Solutions:**\n- Check Node.js version (should be 18+)\n- Clear node_modules: `rm -rf node_modules package-lock.json && npm install`\n- Set CI=false: `CI=false npm run build`\n\n### Issue 2: Wrong Base Directory\n**Symptoms:** Netlify can't find files\n**Solutions:**\n- Set base directory to `web-dashboard` in Netlify settings\n- Or deploy from the correct subdirectory\n\n### Issue 3: Environment Variables\n**Symptoms:** App loads but API calls fail\n**Solutions:**\nAdd these environment variables in Netlify:\n```\nREACT_APP_API_URL=/api\nREACT_APP_ENVIRONMENT=production\nREACT_APP_APP_NAME=Pranayama Coach Admin\nREACT_APP_VERSION=1.0.0\n```\n\n### Issue 4: API Proxy Not Working\n**Symptoms:** Network errors when trying to login\n**Solutions:**\n- Update the backend URL in `netlify.toml` line 23\n- Make sure your backend is deployed and accessible\n- Check CORS settings on your backend\n\n## 📋 Deployment Checklist\n\n- [ ] Node.js 18+ installed\n- [ ] Dependencies installed (`npm install`)\n- [ ] Build succeeds (`npm run build`)\n- [ ] `build/index.html` exists\n- [ ] `build/_redirects` file exists\n- [ ] Netlify base directory set to `web-dashboard`\n- [ ] Environment variables configured\n- [ ] Backend URL updated in netlify.toml\n\n## 🎯 Testing Your Fix\n\n1. **Local Test:**\n   ```bash\n   cd web-dashboard\n   npm start\n   # Visit http://localhost:3000\n   ```\n\n2. **Build Test:**\n   ```bash\n   npm run build\n   npx serve -s build\n   # Visit http://localhost:3000\n   ```\n\n3. **Netlify Test:**\n   - Visit your Netlify URL\n   - Try navigating to `/dashboard`, `/users`, etc.\n   - Check browser console for errors\n\n## 🆘 Still Having Issues?\n\n### Check Netlify Build Logs\n1. Go to Netlify dashboard\n2. Click on your site\n3. Go to \"Deploys\" tab\n4. Click on the latest deploy\n5. Check the build log for errors\n\n### Common Error Messages\n\n**\"Module not found\"**\n- Run `npm install` and rebuild\n\n**\"Command failed with exit code 1\"**\n- Check for TypeScript errors\n- Set `CI=false` in build command\n\n**\"_redirects file not found\"**\n- Make sure `public/_redirects` exists\n- Check if it's copied to build folder\n\n### Test URLs to Try\n\nAfter fixing, test these URLs on your Netlify site:\n- `https://your-site.netlify.app/` (should show login)\n- `https://your-site.netlify.app/dashboard` (should redirect to login)\n- `https://your-site.netlify.app/users` (should redirect to login)\n\n## 📞 Getting Help\n\nIf you're still stuck:\n1. Check the Netlify deploy logs\n2. Test the build locally first\n3. Make sure all files are committed to Git\n4. Try a fresh deploy\n\n---\n\n**Your dashboard should be working after following these steps! 🎉**